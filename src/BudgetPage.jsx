import styled from "styled-components";

const BudgetPage = () => {
  return (
    <Container>
      <div> 안녕</div>
    </Container>
  );
};

export default BudgetPage;

const Container = styled.div`
  display: flex;
`;
